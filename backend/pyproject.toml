[project]
name = "genai-at-scale-backend"
version = "0.1.0"
description = "Minimal FastAPI backend slice for the DoorDash-style assistant MVP."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
